
Repository: web-app
Branch: add-instructions-web-app
Purpose: Front-end application (React/Vite) for Panther presentation.

Prerequisites:
- Node 16+ and npm or Yarn

Setup:
- Install dependencies:
	npm install
	or
	yarn install

Run locally:
- Start dev server:
	npm run dev
	or
	yarn dev

Build:
	npm run build
	or
	yarn build

Tests & linting:
- Run tests (if present) with `npm test` or `yarn test`.
- Lint with `npm run lint` if configured.

Notes:
- Default branch for presentation is `presentation-branch`; this branch is `add-instructions-web-app`.

# web-app — Repository Instructions

Purpose: Internal web application. Keep project settings unchanged.

Prerequisites
- Node.js and yarn/npm

Setup
- `yarn` or `npm install`

Common commands
- Dev: `yarn dev` or `npm run dev`
- Build: `yarn build` (agent will not run builds by default)

Agent guidance
- Agent will not run build/test/lint unless explicitly asked. When modifying code, produce a short todo plan.

CI notes
- Branch: presentation-branch is active in this workspace. Follow PR guidelines.

Owners
- Check `CODEOWNERS`.
